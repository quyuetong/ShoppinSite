import React, { Component } from "react";
import ReactStars from 'react-stars'
import Modal from "./pods/overlay"

class Home extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        //return <ReactStars count={5} size={24} value={3} edit={false}/>
        return <b>Home page, content haven't decide yet.</b>
    }
}

export default Home;